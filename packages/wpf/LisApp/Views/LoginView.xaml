<UserControl x:Class="LisApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <d:DesignerProperties.DesignStyle>
        <Style TargetType="UserControl">
            <Setter Property="Background" Value="White" />
        </Style>
    </d:DesignerProperties.DesignStyle>
    
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <Label x:Name="lblWelcome" Content="{Binding Message}" Margin="20,20,20,0" />
            <Label x:Name="lblUrl" Content="Url" Margin="20,20,20,0"/>
            <TextBox x:Name="txtUrl" Text="{Binding Url}" Margin="20,20,20,0"/>
            <Label x:Name="lblName" Content="Name" Margin="20,20,20,0"/>
            <TextBox x:Name="txtName" Text="{Binding Username}" Margin="20,20,20,0"/>
            <Label x:Name="lblPass" Content="Pass" Margin="20,20,20,0"/>
            <PasswordBox x:Name="txtPassword" Password="1234512345123451234512345" PasswordChanged="PasswordBox_PasswordChanged" PasswordChar="*" Margin="20,20,20,0"/>
            <Button x:Name="button" Content="Login" Command="{Binding NavigateLoginCommand}" Margin="20,20,20,0" HorizontalAlignment="Center"/>

        </StackPanel>

    </Grid>
</UserControl>
